<%= content_tag :div, class: 'media', id: item.id do %>
  <div class="media-body">
    <small>
      <%= item.name %>
      <%= link_to "", [@user, item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %>
    </small>
  </div>
<% end %>